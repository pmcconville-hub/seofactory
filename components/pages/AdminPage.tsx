import React from 'react';
import AdminDashboard from '../admin/AdminDashboard';

/**
 * AdminPage - Route wrapper for the admin dashboard.
 */
const AdminPage: React.FC = () => {
    return <AdminDashboard />;
};

export default AdminPage;
